<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width" />
    <style>
      :root {
        /* https://coolors.co/d0e3cc-f7ffdd-fcfdaf-efd780-dba159 */
        --color1: #DBA159;
        --color2: #EFD780;
        --color3: #FCFDAF;
        --color4: #F7FFDD;
        --color5: #D0E3CC;
        --button-background: var(--color1);
        --button-background-secondary: var(--color2);
        --button-color: #333;
        --secondary-button-background: var(--color5);
        --secondary-button-background-secondary: var(--color2);
        --secondary-button-color: #333;
      }

      html {
        background-color: var(--color4);
        font-family: sans-serif;
      }

      h1 {
        font-size: 2rem;
        font-weight: bold;
        text-align: center;
      }

      .dash-button {
        align-items: center;
        appearance: none;
        background-clip: padding-box;
        background-color: var(--button-background-secondary);
        border-radius: 8px;
        border-style: none;
        box-sizing: border-box;
        color: var(--button-color);
        display: inline-block;
        flex-direction: row;
        flex-shrink: 0;
        font-size: 1em;
        font-weight: 800;
        justify-content: center;
        line-height: 24px;
        min-height: 1em;
        overflow: visible;
        padding: 0.8rem 1.2rem;
        pointer-events: auto;
        position: relative;
        text-align: center;
        text-decoration: none;
        touch-action: manipulation;
        vertical-align: middle;
        width: auto;
        z-index: 0;
      }

      .dash-button:hover:not(:disabled) {
        background-color: initial;
      }

      @media (min-width: 768px) {
        .dash-button {
          padding: 1.2rem 2rem;
        }
      }

      @media (min-width: 1200px) {
        .dash-button {
          padding: 1.2rem 2.2rem;
        }
      }

      .dash-button:before,
      .dash-button:after {
        border-radius: 8px;
      }

      .dash-button:before {
        outline: var(--button-background-secondary) solid 1px;
        content: "";
        height: 100%;
        left: 0;
        overflow: hidden;
        position: absolute;
        top: 0;
        width: 100%;
        z-index: -2;
      }

      .dash-button:after {
        background-color: var(--button-background);
        content: "";
        overflow: hidden;
        position: absolute;
        z-index: -1;
      }

      .dash-button:hover:not(:disabled):after {
        bottom: 0;
        left: 0;
        right: 0;
        top: 0;
      }

      .dash-button:active:not(:disabled):before {
        outline: var(--button-background) solid 1px;
      }

      .dash-button:active:not(:disabled):after {
        bottom: 2px;
        left: 2px;
        right: 2px;
        top: 2px;
      }

      .dash-button:disabled {
        opacity: .24;
      }

      .layout-button {
        color: var(--secondary-button-color);
        background-color: var(--secondary-button-background);
      }

      .layout-button:hover:not(:disabled) {
        background-color: initial;
      }

      .layout-button:before {
        outline: var(--secondary-button-background-secondary) solid 1px;
      }

      .layout-button:after {
        background-color: var(--secondary-button-background-secondary);
      }

      .layout-button:active:not(:disabled):before {
        outline: var(--secondary-button-background-secondary) solid 1px;
      }

      .navigation {
        display: flex;
        flex-wrap: wrap;
        gap: 0.2em;
        justify-content: center;
        margin-bottom: 1.2em;
      }

      .header-section, .content-section {
        margin-left: 2em;
        margin-right: 2em;
      }

      @media (min-width: 768px) {
        .header-section, .content-section {
          margin-left: 3em;
          margin-right: 3em;
        }
      }

      @media (min-width: 1200px) {
        .header-section, .content-section {
          margin-left: 10%;
          margin-right: 10%;
        }
      }

      /* Styling below here should go in a separate template */

      .submit-button {
        padding: 0.5rem;
        left: 100%;
        transform: translateX(calc(-50% - 2rem));
      }

      .submit-button:hover:not(:disabled) {
        background-color: var(--color1);
      }

      input, textarea {
        display: block;
        font-size: 1.2rem;
        margin-top: 1em;
        margin-bottom: 1em;
        width: 100%;
      }
      input:focus, textarea:focus {
        box-shadow: 1px 1px var(--color2);
        outline-style: solid;
        outline-color: var(--color2);
      }
      input::placeholder, textarea::placeholder {
        color: var(--color2);
        font-family: sans-serif;
        font-size: 1rem;
        font-weight: bold;
      }
    </style>
  </head>

  <body>
    <section class="header-section">
      <nav class="navigation">
        <a class="dash-button" href="#">Add recipe</a>
        <a class="dash-button layout-button" href="/">Edit recipe</a>
        <!--<a class="dash-button" href="/dashboard/new-recipe">Add recipe</a>-->
        <a class="dash-button layout-button" href="#">Home layout</a>
        <!--<a class="dash-button layout-button" href="/dashboard/edit-main-layout">Main page</a>-->
        <a class="dash-button layout-button" href="/dashboard/edit-recipe-layout">Recipe layout</a>
        <a class="dash-button layout-button" href="/dashboard/edit-dashboard-layout">This layout</a>
      </nav>
      <header>
        <h1>Add recipe</h1>
      </header>
    </section>

    <section class="content-section">
      <form class="recipe-form" action="/add-recipe" method="post" autocomplete="off">
        <input type="text" placeholder="recipe title" id="title" name="title" autofocus required />
        <textarea placeholder="the recipe" id="recipe" name="recipe" required></textarea>

        <input class="dash-button submit-button" type="submit" value="Create" />
      </form>
    </section>
  </body>
</html>
